@using HelpDeskKyotera.Controllers
@model AdminDashboardViewModel

@{
    ViewData["Title"] = "Admin Dashboard";
}

<style>
    :root {
        --sidebar-width: 280px;
        --primary-color: #2563eb;
        --primary-dark: #1e40af;
        --primary-light: #3b82f6;
        --secondary-color: #10b981;
        --accent-color: #f59e0b;
        --danger-color: #ef4444;
        --warning-color: #f97316;
        --bg-main: #f8fafc;
        --bg-card: #ffffff;
        --bg-sidebar: #1e293b;
        --text-primary: #0f172a;
        --text-secondary: #64748b;
        --text-light: #94a3b8;
        --border-color: #e2e8f0;
        --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
        --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        background: var(--bg-main);
        color: var(--text-primary);
        line-height: 1.6;
        overflow-x: hidden;
    }

    /* Layout */
    .admin-container {
        display: flex;
        min-height: 100vh;
    }

    /* Sidebar */
    .sidebar {
        width: var(--sidebar-width);
        background: linear-gradient(180deg, var(--bg-sidebar) 0%, #0f172a 100%);
        color: white;
        position: fixed;
        height: 100vh;
        left: 0;
        top: 0;
        overflow-y: auto;
        overflow-x: hidden;
        box-shadow: var(--shadow-xl);
        z-index: 1000;
        transition: all 0.3s ease;
        display: flex;
        flex-direction: column;
    }

    .sidebar::-webkit-scrollbar {
        width: 6px;
    }

    .sidebar::-webkit-scrollbar-track {
        background: rgba(255, 255, 255, 0.05);
    }

    .sidebar::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.2);
        border-radius: 3px;
    }

    .sidebar::-webkit-scrollbar-thumb:hover {
        background: rgba(255, 255, 255, 0.3);
    }

    .sidebar-header {
        padding: 2rem 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(255, 255, 255, 0.05);
    }

    .sidebar-header h2 {
        font-size: 1.25rem;
        font-weight: 700;
        margin-bottom: 0.25rem;
        letter-spacing: -0.02em;
        background: linear-gradient(135deg, #ffffff 0%, #e2e8f0 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .sidebar-header p {
        font-size: 0.875rem;
        color: var(--text-light);
        font-weight: 500;
    }

    .user-info {
        padding: 1.25rem 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(37, 99, 235, 0.1);
    }

    .user-info p {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--text-light);
        margin-bottom: 0.5rem;
        font-weight: 600;
    }

    .user-info span {
        font-size: 0.95rem;
        font-weight: 600;
        color: #ffffff;
    }

    .nav-menu {
        padding: 1.5rem 0;
    }

    .nav-menu ul {
        list-style: none;
        overflow-y: auto !important;
        overflow-x: hidden !important;
        width: 100% !important;
    }

    .nav-menu li {
        margin-bottom: 0.25rem;
        width: 100% !important;
    }

    .nav-menu a {
        display: flex;
        align-items: center;
        padding: 0.875rem 1.5rem;
        color: rgba(255, 255, 255, 0.8);
        text-decoration: none;
        transition: all 0.2s ease;
        font-size: 0.95rem;
        font-weight: 500;
        position: relative;
    }

    .nav-menu a::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0;
        bottom: 0;
        width: 4px;
        background: var(--primary-light);
        transform: scaleY(0);
        transition: transform 0.2s ease;
    }

    .nav-menu a:hover {
        background: rgba(255, 255, 255, 0.08);
        color: #ffffff;
        padding-left: 2rem;
    }

    .nav-menu a:hover::before {
        transform: scaleY(1);
    }

    .nav-menu a.active {
        background: rgba(37, 99, 235, 0.2);
        color: #ffffff;
        font-weight: 600;
    }

    .nav-menu a.active::before {
        transform: scaleY(1);
    }

    .quick-actions {
        padding: 1.5rem;
        margin: 1rem 1.5rem;
        background: rgba(16, 185, 129, 0.1);
        border-radius: 12px;
        border: 1px solid rgba(16, 185, 129, 0.2);
    }

    .quick-actions h3 {
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: 1rem;
        color: var(--secondary-color);
        font-weight: 700;
    }

    .quick-actions ul {
        list-style: none;
    }

    .quick-actions li {
        margin-bottom: 0.5rem;
    }

    .quick-actions a {
        display: block;
        padding: 0.625rem 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 8px;
        color: rgba(255, 255, 255, 0.9);
        text-decoration: none;
        font-size: 0.875rem;
        transition: all 0.2s ease;
        font-weight: 500;
    }

    .quick-actions a:hover {
        background: rgba(255, 255, 255, 0.12);
        transform: translateX(4px);
        color: #ffffff;
    }

    .logout-btn {
        margin: 1.5rem;
        padding: 0.875rem 1.5rem;
        background: linear-gradient(135deg, var(--danger-color) 0%, #dc2626 100%);
        color: white;
        text-decoration: none;
        display: block;
        text-align: center;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }

    .logout-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(239, 68, 68, 0.4);
    }

    /* Main Content */
    .main-content {
        flex: 1;
        margin-left: var(--sidebar-width);
        padding: 2rem;
        background: var(--bg-main);
    }

    /* Collapsed sidebar support */
    .sidebar.collapsed {
        transform: translateX(-100%);
    }

    .main-content.expanded {
        margin-left: 0;
    }

    .dashboard-header {
        margin-bottom: 2.5rem;
    }

    .dashboard-header h1 {
        font-size: 2rem;
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: 0.5rem;
        letter-spacing: -0.03em;
    }

    .dashboard-header .date {
        font-size: 0.95rem;
        color: var(--text-secondary);
        font-weight: 500;
    }

    /* Alerts */
    .alert {
        padding: 1rem 1.5rem;
        border-radius: 12px;
        margin-bottom: 1.5rem;
        font-weight: 500;
        box-shadow: var(--shadow-md);
    }

    .alert-error {
        background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%);
        border-left: 4px solid var(--danger-color);
        color: #991b1b;
    }

    .alert-success {
        background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
        border-left: 4px solid var(--secondary-color);
        color: #166534;
    }

    /* Stats Grid */
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2.5rem;
    }

    .stat-card {
        background: var(--bg-card);
        padding: 2rem;
        border-radius: 16px;
        box-shadow: var(--shadow-md);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        border: 1px solid var(--border-color);
    }

    .stat-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, var(--primary-color) 0%, var(--primary-light) 100%);
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.5s ease;
    }

    .stat-card:hover {
        transform: translateY(-4px);
        box-shadow: var(--shadow-xl);
    }

    .stat-card:hover::before {
        transform: scaleX(1);
    }

    .stat-card:nth-child(1)::before {
        background: linear-gradient(90deg, #2563eb 0%, #3b82f6 100%);
    }

    .stat-card:nth-child(2)::before {
        background: linear-gradient(90deg, #10b981 0%, #34d399 100%);
    }

    .stat-card:nth-child(3)::before {
        background: linear-gradient(90deg, #f59e0b 0%, #fbbf24 100%);
    }

    .stat-card h3 {
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--text-secondary);
        margin-bottom: 1rem;
        font-weight: 700;
    }

    .stat-value {
        font-size: 2.5rem;
        font-weight: 800;
        color: var(--text-primary);
        margin-bottom: 1rem;
        letter-spacing: -0.02em;
    }

    .stat-link {
        display: inline-flex;
        align-items: center;
        color: var(--primary-color);
        text-decoration: none;
        font-size: 0.875rem;
        font-weight: 600;
        transition: all 0.2s ease;
    }

    .stat-link:hover {
        color: var(--primary-dark);
        transform: translateX(4px);
    }

    .stat-link::after {
        content: '→';
        margin-left: 0.5rem;
        transition: margin-left 0.2s ease;
    }

    .stat-link:hover::after {
        margin-left: 0.75rem;
    }

    /* Content Grid */
    .content-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 2rem;
        margin-bottom: 2rem;
    }

    /* Card */
    .card {
        background: var(--bg-card);
        border-radius: 16px;
        box-shadow: var(--shadow-md);
        overflow: hidden;
        border: 1px solid var(--border-color);
    }

    .card-header {
        padding: 1.5rem 2rem;
        border-bottom: 1px solid var(--border-color);
        background: linear-gradient(135deg, #fafbfc 0%, #ffffff 100%);
    }

    .card-header h2 {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--text-primary);
        letter-spacing: -0.02em;
    }

    .card-body {
        padding: 2rem;
    }

    /* Table */
    .table-container {
        overflow-x: auto;
    }

    table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
    }

    thead {
        background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
    }

    thead th {
        padding: 1rem 1.5rem;
        text-align: left;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--text-secondary);
        font-weight: 700;
        border-bottom: 2px solid var(--border-color);
    }

    tbody tr {
        transition: all 0.2s ease;
        border-bottom: 1px solid var(--border-color);
    }

    tbody tr:hover {
        background: linear-gradient(135deg, #fafbfc 0%, #f8fafc 100%);
    }

    tbody td {
        padding: 1.25rem 1.5rem;
        font-size: 0.9375rem;
        color: var(--text-primary);
    }

    .ticket-number {
        font-family: 'Space Mono', monospace;
        font-weight: 700;
        color: var(--primary-color);
        font-size: 0.875rem;
    }

    .ticket-title {
        font-weight: 600;
        color: var(--text-primary);
    }

    /* Status Badge */
    .status-badge {
        display: inline-block;
        padding: 0.375rem 0.875rem;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .status-open {
        background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%);
        color: #1e40af;
    }

    .status-progress {
        background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
        color: #92400e;
    }

    .status-closed {
        background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
        color: #065f46;
    }

    .status-pending {
        background: linear-gradient(135deg, #fed7aa 0%, #fdba74 100%);
        color: #9a3412;
    }

    /* Priority Badge */
    .priority-high {
        color: var(--danger-color);
        font-weight: 700;
    }

    .priority-medium {
        color: var(--warning-color);
        font-weight: 600;
    }

    .priority-low {
        color: var(--secondary-color);
        font-weight: 500;
    }

    /* Buttons */
    .btn {
        display: inline-block;
        padding: 0.5rem 1.25rem;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        font-size: 0.875rem;
        transition: all 0.2s ease;
        cursor: pointer;
        border: none;
    }

    .btn-primary {
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
        color: white;
        box-shadow: 0 2px 8px rgba(37, 99, 235, 0.3);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(37, 99, 235, 0.4);
    }

    .btn-secondary {
        background: var(--bg-card);
        color: var(--text-primary);
        border: 2px solid var(--border-color);
    }

    .btn-secondary:hover {
        background: var(--bg-main);
        border-color: var(--primary-color);
        color: var(--primary-color);
    }

    .btn-sm {
        padding: 0.375rem 1rem;
        font-size: 0.8125rem;
    }

    /* Department Cards */
    .departments-list {
        display: grid;
        gap: 1rem;
    }

    .department-card {
        padding: 1.25rem 1.5rem;
        background: linear-gradient(135deg, #fafbfc 0%, #ffffff 100%);
        border-radius: 12px;
        border: 1px solid var(--border-color);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

    .department-card::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0;
        bottom: 0;
        width: 4px;
        background: linear-gradient(180deg, var(--primary-color) 0%, var(--secondary-color) 100%);
        transform: scaleY(0);
        transform-origin: bottom;
        transition: transform 0.3s ease;
    }

    .department-card:hover {
        transform: translateX(4px);
        box-shadow: var(--shadow-md);
    }

    .department-card:hover::before {
        transform: scaleY(1);
    }

    .department-name {
        font-size: 1.125rem;
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: 0.5rem;
        letter-spacing: -0.01em;
    }

    .department-location {
        font-size: 0.875rem;
        color: var(--text-secondary);
        margin-bottom: 0.5rem;
        font-weight: 500;
    }

    .department-users {
        font-size: 0.8125rem;
        color: var(--primary-color);
        font-weight: 600;
    }

    /* Empty State */
    .empty-state {
        text-align: center;
        padding: 3rem 2rem;
        color: var(--text-secondary);
    }

    .empty-state p {
        font-size: 1rem;
        font-weight: 500;
    }

    /* Date Text */
    .date-text {
        font-size: 0.875rem;
        color: var(--text-secondary);
        font-weight: 500;
    }

    /* View All Link */
    .view-all-link {
        display: block;
        text-align: center;
        padding: 1rem;
        color: var(--primary-color);
        text-decoration: none;
        font-weight: 600;
        font-size: 0.9375rem;
        border-top: 1px solid var(--border-color);
        transition: all 0.2s ease;
    }

    .view-all-link:hover {
        background: linear-gradient(135deg, #fafbfc 0%, #f8fafc 100%);
        color: var(--primary-dark);
    }



    /* Responsive adjustments */
    .responsive-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
    }

    /* Utility Classes */
    .text-center {
        text-align: center;
    }

    .mb-1 {
        margin-bottom: 0.5rem;
    }

    .mb-2 {
        margin-bottom: 1rem;
    }

    .mb-3 {
        margin-bottom: 1.5rem;
    }

    .mt-1 {
        margin-top: 0.5rem;
    }

    .mt-2 {
        margin-top: 1rem;
    }

    .mt-3 {
        margin-top: 1.5rem;
    }

    .fw-bold {
        font-weight: 700;
    }

    .fw-semibold {
        font-weight: 600;
    }

    .text-muted {
        color: var(--text-secondary);
    }

    /* ========== SIDEBAR DESIGN IMPROVEMENTS ========== */
    .sidebar-header {
        padding: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        background: linear-gradient(135deg, rgba(37, 99, 235, 0.1) 0%, rgba(59, 130, 246, 0.05) 100%);
    }

    .sidebar-brand {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .sidebar-brand i {
        font-size: 1.5rem;
    }

    .sidebar-user-info {
        padding: 1rem 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        background: rgba(16, 185, 129, 0.08);
    }

    .sidebar-user-info small {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        font-weight: 600;
    }

    .sidebar-content {
        padding: 0;
    }

    .sidebar-section-title {
        padding: 0.75rem 1.5rem;
        font-size: 0.7rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.06em;
        color: rgba(255, 255, 255, 0.4);
        margin-top: 0.75rem;
        margin-bottom: 0.5rem;
        display: block;
    }

    .sidebar-nav-section,
    .sidebar-quick-actions {
        width: 100%;
    }

    .sidebar-nav-section ul {
        list-style: none;
        padding: 0;
    }

    .sidebar-nav-section ul li {
        margin: 0;
    }

    .sidebar-quick-actions {
        border-top: 1px solid rgba(255, 255, 255, 0.08);
        padding-top: 0.75rem;
    }

    .sidebar-footer {
        padding: 1rem 1.5rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        background: linear-gradient(135deg, rgba(239, 68, 68, 0.1) 0%, rgba(220, 38, 38, 0.05) 100%);
    }

    .sidebar-footer .btn {
        width: 100%;
        background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        border: none !important;
        color: white !important;
        transition: all 0.2s ease;
        font-weight: 600;
        font-size: 0.875rem;
    }

    .sidebar-footer .btn:hover {
        background: linear-gradient(135deg, #f87171 0%, #ef4444 100%) !important;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(239, 68, 68, 0.4);
    }

    /* Navigation Links Styling */
    .sidebar .nav-link {
        color: rgba(255, 255, 255, 0.8) !important;
        margin: 0 0.5rem;
        border-radius: 0.375rem !important;
        transition: all 0.2s ease !important;
        font-weight: 500;
        font-size: 0.9375rem;
    }

    .sidebar .nav-link:hover {
        background: rgba(255, 255, 255, 0.1) !important;
        color: white !important;
        transform: translateX(4px);
        padding-left: calc(0.75rem + 4px) !important;
    }

    .sidebar .nav-link.bg-primary {
        background: linear-gradient(135deg, rgba(37, 99, 235, 0.3) 0%, rgba(59, 130, 246, 0.2) 100%) !important;
        color: white !important;
    }

    /* Navigation Menu - Vertical Scrolling Only */
    .nav-menu ul {
        list-style: none;
        overflow-y: auto !important;
        overflow-x: hidden !important;
        width: 100% !important;
    }

    .nav-menu li {
        margin-bottom: 0.25rem;
        width: 100% !important;
    }

    .nav-link {
        position: relative;
        white-space: normal !important;
        word-wrap: break-word !important;
        overflow-wrap: break-word !important;
        display: flex !important;
        align-items: center !important;
        width: 100% !important;
        max-width: 100% !important;
    }

    .nav-link i {
        min-width: 1.25rem;
        text-align: center;
        flex-shrink: 0;
    }

    /* Sidebar Toggle */
    .sidebar-toggle {
        position: fixed !important;
        top: 0.75rem !important;
        left: 0.75rem !important;   /* Slightly inset from the corner */
        width: 30px !important;   /* Smaller button */
        height: 30px !important;
        min-width: 30px !important;
        z-index: 1002 !important; /* Above sidebar */
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%) !important;
        color: white !important;
        border: none !important;
        border-radius: 50% !important;
        display: flex !important;
        align-items: center !important;
        justify-content: center !important;
        cursor: pointer !important;
        transition: all 0.18s ease !important;
        font-size: 0.95rem !important;
        padding: 0 !important;
        box-shadow: 0 3px 8px rgba(37, 99, 235, 0.25) !important;
    }

    .sidebar-toggle:hover {
        transform: translateY(-2px) !important;
        box-shadow: 0 6px 20px rgba(37, 99, 235, 0.4) !important;
    }

    .sidebar-toggle:focus {
        outline: none !important;
    }

    .sidebar-toggle.collapsed {
        left: 0.75rem !important;    /* Stay in same position when collapsed */
    }

    #sidebar.collapsed {
        width: 0 !important;
        overflow: hidden !important;
        box-shadow: none !important;
    }

    #mainContent.expanded {
        margin-left: 0 !important;
        width: 100% !important;
    }

    /* Sidebar improvements */

</style>

<div class="admin-container" style="min-height: 100vh; background: var(--bg-main);">
    <!-- Sidebar Toggle Button -->
    <button class="sidebar-toggle" id="sidebarToggle" title="Toggle Sidebar">
        <i class="bi bi-chevron-left"></i>
    </button>

    <!-- Sidebar -->
    <nav class="bg-dark text-white shadow-lg d-flex flex-column" id="sidebar" style="width: 280px; position: fixed; top: 0; left: 0; height: 100vh; overflow: hidden; z-index: 1000;">
        <!-- Header - Fixed -->
        <div class="sidebar-header flex-shrink-0">
            <div class="sidebar-brand">
                <i class="bi bi-shield-check text-primary"></i>
                <div>
                    <h5 class="mb-1 fw-bold">Admin Panel</h5>
                    <small class="text-white-50">Kyotera System</small>
                </div>
            </div>
        </div>

        <!-- User Info - Fixed -->
        <div class="sidebar-user-info flex-shrink-0">
            <div class="mb-0">
                <small class="text-white-50 d-block mb-2">Logged in as</small>
                <div class="d-flex align-items-center">
                    <i class="bi bi-person-circle me-2 text-success"></i>
                    <strong class="text-white text-truncate">@User.Identity?.Name</strong>
                </div>
            </div>
        </div>

        <!-- Scrollable Content Section -->
        <div class="sidebar-content flex-grow-1" style="overflow-y: auto; overflow-x: hidden; display: flex; flex-direction: column;">
            <!-- Navigation Menu -->
            <div class="sidebar-nav-section">
                <div class="sidebar-section-title">Navigation</div>
                <ul class="nav flex-column" style="padding: 0;">
                    <li class="nav-item">
                        <a class="nav-link text-white rounded-2 px-3 py-2 @(ViewContext.RouteData.Values["action"]?.ToString() == "Index" && ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "bg-primary bg-opacity-25" : "")" 
                           asp-action="Index" asp-contr  'oller="Home" style="transition: all 0.2s ease;">
                            <i class="bi bi-speedometer2 me-2"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white rounded-2 px-3 py-2" asp-controller="Users" asp-action="Index" style="transition: all 0.2s ease;">
                            <i class="bi bi-people me-2"></i> Users
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white rounded-2 px-3 py-2" asp-controller="Departments" asp-action="Index" style="transition: all 0.2s ease;">
                            <i class="bi bi-building me-2"></i> Departments
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white rounded-2 px-3 py-2" asp-controller="Roles" asp-action="Index" style="transition: all 0.2s ease;">
                            <i class="bi bi-shield-lock me-2"></i> Roles
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white rounded-2 px-3 py-2" asp-controller="Categories" asp-action="Index" style="transition: all 0.2s ease;">
                            <i class="bi bi-tags me-2"></i> Categories
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white rounded-2 px-3 py-2" asp-controller="Tickets" asp-action="Index" style="transition: all 0.2s ease;">
                            <i class="bi bi-ticket-perforated me-2"></i> All Tickets
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white rounded-2 px-3 py-2" asp-controller="Claims" asp-action="Index" style="transition: all 0.2s ease;">
                            <i class="bi bi-file-earmark-text me-2"></i> Claims
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white rounded-2 px-3 py-2" asp-controller="DataSeeding" asp-action="Index" style="transition: all 0.2s ease;">
                            <i class="bi bi-database-gear me-2"></i> Data Seeding
                        </a>
                    </li>
                </ul>
            </div>

            <!-- Quick Actions Section -->
            <div class="sidebar-quick-actions">
                <div class="sidebar-section-title">Quick Actions</div>
                <div class="dropdown" style="padding: 0 0.5rem;">
                    <a class="nav-link text-white rounded-2 px-3 py-2 dropdown-toggle d-flex align-items-center" href="#" role="button" id="quickActionsDropdown" data-bs-toggle="dropdown" aria-expanded="false" style="transition: all 0.2s ease;">
                        <i class="bi bi-lightning-charge me-2"></i>
                        <span>Actions</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark mt-2" aria-labelledby="quickActionsDropdown">
                        <li><a class="dropdown-item" asp-controller="Users" asp-action="Create"><i class="bi bi-person-plus me-2"></i> Add User</a></li>
                        <li><a class="dropdown-item" asp-controller="Departments" asp-action="Create"><i class="bi bi-building-add me-2"></i> Department</a></li>
                        <li><a class="dropdown-item" asp-controller="Roles" asp-action="Create"><i class="bi bi-shield-lock me-2"></i> Role</a></li>
                        <li><a class="dropdown-item" asp-controller="Categories" asp-action="Create"><i class="bi bi-folder-plus me-2"></i> Category</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" asp-controller="Tickets" asp-action="AssignmentManagement"><i class="bi bi-arrow-left-right me-2"></i> Assign Tickets</a></li>
                        <li><a class="dropdown-item" asp-controller="Tickets" asp-action="Index"><i class="bi bi-ticket-perforated me-2"></i> Manage Tickets</a></li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Logout Button - Fixed at Bottom -->
        <div class="sidebar-footer flex-shrink-0">
            <form asp-controller="Account" asp-action="Logout" method="post" class="d-grid">
                <button type="submit" class="btn btn-danger btn-sm" style="border-radius: 0.375rem; padding: 0.5rem 1rem; font-size: 0.875rem;">
                    <i class="bi bi-box-arrow-right me-1"></i> Logout
                </button>
            </form>
        </div>
    </nav>

    <!-- Main Content -->
    <main id="mainContent" class="main-content" style="transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);">
        <!-- Top Bar -->

        <!-- Page Content -->
        <div class="container-fluid p-5">

            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger alert-dismissible fade show rounded-3 shadow-sm" role="alert">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i> @TempData["Error"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }

            @if (TempData["Success"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show rounded-3 shadow-sm" role="alert">
                    <i class="bi bi-check-circle-fill me-2"></i> @TempData["Success"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }

            <!-- Stats Cards -->
            <div class="row g-4 mb-5">
                <div class="col-12">
                    <div class="d-flex justify-content-end mb-3">
                        <a href="/Charts" class="btn btn-sm btn-outline-light">
                            <i class="bi bi-bar-chart-fill me-1"></i> Charts
                        </a>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card border-0 shadow-sm h-100 bg-gradient-start-primary rounded-4 overflow-hidden">
                        <div class="card-body text-white">
                            <div class="d-flex align-items-center">
                                <div>
                                    <h6 class="mb-1 text-white-50">Total Tickets</h6>
                                    <h2 class="mb-0 fw-bold">@Model.TotalTickets</h2>
                                </div>
                                <div class="ms-auto">
                                    <i class="bi bi-ticket-perforated" style="font-size: 4rem; opacity: 0.3;"></i>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-white bg-opacity-10 border-0">
                            <a asp-controller="Tickets" asp-action="Index" class="text-white small text-decoration-none fw-medium">
                                View all tickets <i class="bi bi-arrow-right"></i>
                            </a>
                        </div>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="card border-0 shadow-sm h-100 bg-gradient-start-success rounded-4 overflow-hidden">
                        <div class="card-body text-white">
                            <div class="d-flex align-items-center">
                                <div>
                                    <h6 class="mb-1 text-white-50">Departments</h6>
                                    <h2 class="mb-0 fw-bold">@Model.TotalDepartments</h2>
                                </div>
                                <div class="ms-auto">
                                    <i class="bi bi-building" style="font-size: 4rem; opacity: 0.3;"></i>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-white bg-opacity-10 border-0">
                            <a asp-controller="Departments" asp-action="Index" class="text-white small text-decoration-none fw-medium">
                                View all departments <i class="bi bi-arrow-right"></i>
                            </a>
                        </div>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="card border-0 shadow-sm h-100 bg-gradient-start-info rounded-4 overflow-hidden">
                        <div class="card-body text-white">
                            <div class="d-flex align-items-center">
                                <div>
                                    <h6 class="mb-1 text-white-50">Total Users</h6>
                                    <h2 class="mb-0 fw-bold">@Model.TotalUsers</h2>
                                </div>
                                <div class="ms-auto">
                                    <i class="bi bi-people" style="font-size: 4rem; opacity: 0.3;"></i>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-white bg-opacity-10 border-0">
                            <a asp-controller="Users" asp-action="Index" class="text-white small text-decoration-none fw-medium">
                                View all users <i class="bi bi-arrow-right"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Recent Tickets & Quick Actions -->
            <div class="row g-4">
                <div class="col-lg-8">
                    <div class="card border-0 shadow-sm rounded-4">
                        <div class="card-header bg-white border-0 py-3">
                            <h5 class="mb-0 fw-bold text-dark">
                                <i class="bi bi-clock-history text-primary me-2"></i> Recent Tickets
                            </h5>
                        </div>
                        <div class="card-body">
                            @if (Model.RecentTickets.Any())
                            {
                                <div class="table-responsive">
                                    <table class="table table-hover align-middle mb-0">
                                        <thead class="table-light">
                                            <tr>
                                                <th>Ticket #</th>
                                                <th>Title</th>
                                                <th>Status</th>
                                                <th>Priority</th>
                                                <th>Date</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var ticket in Model.RecentTickets)
                                            {
                                                <tr>
                                                    <td><span class="fw-bold text-primary">#@ticket.TicketNumber</span></td>
                                                    <td>@ticket.Title</td>
                                                    <td>
                                                        @if (ticket.Status != null)
                                                        {
                                                            <span class="badge rounded-pill px-3 py-2 bg-@(ticket.Status.Name == "Open" ? "success" : ticket.Status.Name == "Closed" ? "secondary" : "warning")">
                                                                @ticket.Status.Name
                                                            </span>
                                                        }
                                                    </td>
                                                    <td><span class="badge bg-light text-dark">N/A</span></td>
                                                    <td><small class="text-muted">@ticket.CreatedOn.ToString("MMM dd, yyyy")</small></td>
                                                    <td>
                                                        <a asp-controller="Tickets" asp-action="Details" asp-route-id="@ticket.TicketId" 
                                                           class="btn btn-sm btn-outline-primary rounded-pill px-3">View</a>
                                                    </td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            }
                            else
                            {
                                <p class="text-muted text-center py-4">No recent tickets found.</p>
                            }
                        </div>
                        <div class="card-footer bg-light border-0 text-end py-3">
                            <a asp-controller="Tickets" asp-action="Index" class="btn btn-primary rounded-pill px-4">View All Tickets</a>
                        </div>
                    </div>
                </div>

                <div class="col-lg-4">

                    <!-- Analytics Charts -->
                    <div class="card border-0 shadow-sm rounded-4 mb-4">
                        <div class="card-header bg-white border-0 py-3">
                            <h5 class="mb-0 fw-bold text-dark"><i class="bi bi-graph-up me-2 text-primary"></i>Analytics</h5>
                        </div>
                        <div class="card-body">
                            <div class="mb-3">
                                <canvas id="ticketsByStatusChart" height="200"></canvas>
                            </div>
                            <div>
                                <canvas id="ticketsByMonthChart" height="160"></canvas>
                            </div>
                        </div>
                    </div>


                    <!-- Departments -->
                    <div class="card border-0 shadow-sm rounded-4">
                        <div class="card-header bg-white border-0 py-3">
                            <h5 class="mb-0 fw-bold text-dark">
                                <i class="bi bi-building text-success me-2"></i> Departments
                            </h5>
                        </div>
                        <div class="card-body">
                            @if (Model.Departments.Any())
                            {
                                <div class="row g-3 p-3">
                                    @foreach (var dept in Model.Departments.Take(6))
                                    {
                                        <div class="col-12 col-md-6">
                                            <div class="department-card d-flex align-items-center justify-content-between">
                                                <div>
                                                    <h6 class="mb-1 fw-semibold">@dept.Name</h6>
                                                    <div class="small text-muted">@(dept.Location?.Name ?? "No location")</div>
                                                </div>
                                                <div class="text-end">
                                                    <div class="mb-2">
                                                        <span class="badge bg-primary rounded-pill">@dept.Users.Count users</span>
                                                    </div>
                                                    <a asp-controller="Departments" asp-action="Details" asp-route-id="@dept.DepartmentId" class="btn btn-sm btn-outline-primary">Manage</a>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                            }
                            else
                            {
                                <p class="text-muted text-center py-4 mb-0">No departments available.</p>
                            }
                        </div>
                        <div class="card-footer bg-light border-0 text-end py-3">
                            <a asp-controller="Departments" asp-action="Index" class="btn btn-outline-primary rounded-pill px-4">View All</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</div>

<style>
    /* Custom Gradient Backgrounds */
    .bg-gradient-start-primary {
        background: linear-gradient(135deg, #0d6efd 0%, #0d6efd 50%, #0d5bfd 100%);
    }
    .bg-gradient-start-success {
        background: linear-gradient(135deg, #198754 0%, #157347 100%);
    }
    .bg-gradient-start-info {
        background: linear-gradient(135deg, #0dcaf0 0%, #0db8d8 100%);
    }

    /* Sidebar Improvements */
    .nav-link {
        padding: 0.75rem 1rem !important;
        transition: all 0.3s ease;
        border-radius: 0.5rem !important;
        margin: 0.25rem 0.5rem;
    }

    .nav-link:hover {
        background-color: rgba(255, 255, 255, 0.15) !important;
        padding-left: 1.5rem !important;
        transform: translateX(4px);
    }

    .nav-link.active {
        background-color: rgba(13, 110, 253, 0.25) !important;
        font-weight: 600;
    }

    /* Card & Button Enhancements */
    .card {
        transition: all 0.3s ease;
        border-radius: 1rem !important;
    }

    .card:hover {
        transform: translateY(-6px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1) !important;
    }

    .btn {
        transition: all 0.3s ease;
        font-weight: 500;
    }

    .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }

    /* Scrollbar */
    nav::-webkit-scrollbar {
        width: 6px;
    }
    nav::-webkit-scrollbar-track {
        background: transparent;
    }
    nav::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.3);
        border-radius: 3px;
    }
    nav::-webkit-scrollbar-thumb:hover {
        background: rgba(255, 255, 255, 0.5);
    }

    /* Badge improvements */
    .badge {
        font-weight: 600;
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const sidebarToggle = document.getElementById('sidebarToggle');
        const sidebar = document.getElementById('sidebar');
        const mainContent = document.getElementById('mainContent');

        if (!sidebarToggle || !sidebar || !mainContent) {
            console.error('Required elements not found');
            return;
        }

        let isCollapsed = localStorage.getItem('sidebarCollapsed') === 'true';

        function updateSidebarState() {
            if (isCollapsed) {
                sidebar.classList.add('collapsed');
                mainContent.classList.add('expanded');
                sidebarToggle.classList.add('collapsed');
                sidebarToggle.innerHTML = '<i class="bi bi-chevron-right"></i>';
            } else {
                sidebar.classList.remove('collapsed');
                mainContent.classList.remove('expanded');
                sidebarToggle.classList.remove('collapsed');
                sidebarToggle.innerHTML = '<i class="bi bi-chevron-left"></i>';
            }
        }

        // Initialize state on page load
        updateSidebarState();

        // Toggle sidebar on button click
        sidebarToggle.addEventListener('click', function(e) {
            e.preventDefault();
            isCollapsed = !isCollapsed;
            localStorage.setItem('sidebarCollapsed', isCollapsed);
            updateSidebarState();
        });
    });
</script>
<!-- Chart.js CDN and rendering script -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    (function(){
        // Data from server
        var statusLabels = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.TicketsByStatusLabels ?? new List<string>()));
        var statusData = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.TicketsByStatusData ?? new List<int>()));

        var monthLabels = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.TicketsByMonthLabels ?? new List<string>()));
        var monthData = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.TicketsByMonthData ?? new List<int>()));

        // Tickets by status - doughnut
        var ctx1 = document.getElementById('ticketsByStatusChart');
        if (ctx1) {
            new Chart(ctx1, {
                type: 'doughnut',
                data: {
                    labels: statusLabels,
                    datasets: [{
                        data: statusData,
                        backgroundColor: [ '#0d6efd', '#20c997', '#ffc107', '#dc3545', '#6c757d' ],
                        borderWidth: 0
                    }]
                },
                options: {
                    plugins: { legend: { position: 'bottom' } },
                    maintainAspectRatio: false
                }
            });
        }

        // Tickets by month - bar
        var ctx2 = document.getElementById('ticketsByMonthChart');
        if (ctx2) {
            new Chart(ctx2, {
                type: 'bar',
                data: {
                    labels: monthLabels,
                    datasets: [{
                        label: 'Tickets',
                        data: monthData,
                        backgroundColor: '#2563eb'
                    }]
                },
                options: {
                    plugins: { legend: { display: false } },
                    scales: { y: { beginAtZero: true } },
                    maintainAspectRatio: false
                }
            });
        }
    })();
</script>
